name: photo_frame
description: Photo frame software.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  transparent_image: ^2.0.0
  palette_generator: ^0.3.0
  cupertino_icons: ^1.0.3
  google_fonts: ^2.0.0
  intl: ^0.17.0
  decorated_icon: ^1.2.0
  toml: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
